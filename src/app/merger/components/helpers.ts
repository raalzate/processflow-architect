// Utilidades para fusión de grafos.
// La implementación vive en src/lib/graph-merge.ts (lógica pura con cobertura);
// aquí sólo se re-exporta para mantener compatibilidad con imports existentes.
export { updateEdgesForMerge, cleanupDuplicateEdges } from "@/lib/graph-merge";
